[project]
name = "humanify"
version = "1.0"
authors = [{name = "wonskur"}]
description = "A human control simulation"
readme = "README.md"
license = "GPL-3.0"
requires-python = ">=3.10"
[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"